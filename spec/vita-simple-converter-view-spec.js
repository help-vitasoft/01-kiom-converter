'use babel';

import VitaSimpleConverterView from '../lib/vita-simple-converter-view';

describe('VitaSimpleConverterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
